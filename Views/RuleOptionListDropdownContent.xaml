<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="lunagalLauncher.Views.RuleOptionListDropdownContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer x:Name="ScrollRoot"
                  VerticalScrollBarVisibility="Auto"
                  Padding="16,12,16,12">
        <ItemsRepeater x:Name="OptionItemsRepeater">
            <ItemsRepeater.Layout>
                <StackLayout Orientation="Vertical"/>
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate>
                    <Border Background="Transparent" Margin="0,2" CornerRadius="6" Padding="12,8"
                            Tapped="OptionItemBorder_Tapped"
                            PointerEntered="OptionItemBorder_PointerEntered"
                            PointerExited="OptionItemBorder_PointerExited">
                        <TextBlock Text="{Binding}" FontSize="13" TextWrapping="Wrap" LineHeight="20"/>
                    </Border>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
    </ScrollViewer>
</UserControl>
